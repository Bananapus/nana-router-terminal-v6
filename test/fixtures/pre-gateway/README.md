# Pre-gateway deployment fixture

These minimal artifacts retain the Base registry and previous router addresses used before the gateway rollout. The deployment reader consumes only `.address`. The gateway artifact is deliberately absent so the compatibility test remains independent of current production deployment records.
